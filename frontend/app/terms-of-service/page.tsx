import Form from '@/components/update-profile-form/form';
// import { redirect } from 'next/navigation';

const page = () => {
	return (
		<div className=' h-screen flex items-center'>
			<Form />
		</div>
	);
};

export default page;
